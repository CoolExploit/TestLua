local P = {}

function P.touch(p)
    local r = P.r()
    if not r or not p then return end
    local o = r.CFrame
    r.CFrame = p.CFrame * CFrame.new(0,0,-0.2)
    task.wait()
    r.CFrame = o
end

return P
